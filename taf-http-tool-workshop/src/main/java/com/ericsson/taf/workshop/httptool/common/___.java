package com.ericsson.taf.workshop.httptool.common;

/**
 * Placeholder that allows source to compile, but needs to be replaced with real code to solve exercise
 */
public class ___ {
    public static <T> T ___() {
        StackTraceElement stackTrace = new Exception().getStackTrace()[1];
        throw new Error("Please replace placeholder ___ at " + stackTrace);
    }
}
